#Keybindings
bind pager < previous-line
bind pager > next-line
bind pager <backspace> previous-line
bind pager <delete> previous-line
bind pager \cb half-up
bind pager \cf half-down
bind pager R reply
bind pager , delete-message
bind pager $ bottom
bind pager <Tab> next-new
bind compose v view-attach
bind compose \n send-message
bind compose s send-message
bind compose S edit-subject
bind index - previous-page
bind index , delete-subthread
bind index . undelete-subthread
bind index r reply
bind pager r reply
bind pager \cr list-reply
bind index R reply
bind index \cr list-reply
bind index Q quit
bind attach Q exit
bind attach <Space> view-attach
bind index y noop
#bind index \040 next-page

